android {
    namespace "com.example.notes_calendar_fpt"
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.example.notes_calendar_fpt"
        minSdkVersion 24
        targetSdkVersion 34
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            // R8/proguard mặc định của Flutter
            minifyEnabled true
            shrinkResources true
            signingConfig signingConfigs.release // cấu hình khi ký
        }
        debug {
            minifyEnabled false
        }
    }
}
